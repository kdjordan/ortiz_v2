# Host node_modules holds darwin-arm64 sharp binaries — must NOT overwrite the
# linux binaries installed by `npm ci` in the image build.
node_modules
# Ephemeral working clone (recreated on boot).
work
# Secrets never belong in the image.
.env
.env.*
# Test suite isn't needed at runtime.
test
npm-debug.log*
